{
	"name": "snake_multiplayer",
	"version": "1.0.0",
	"description": "",
	"main": "index.js",
	"scripts": {
		"watch": "nodemon --config nodemon.json",
		"live-server": "live-server --port=8000 client/",
		"test": "echo \"Error: no test specified\" && exit 1"
	},
	"keywords": [],
	"author": "",
	"license": "ISC",
	"dependencies": {
		"ws": "^7.2.1"
	}
}
